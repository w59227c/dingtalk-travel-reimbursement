# 项目协作说明

本文件适用于整个仓库。用户当前请求优先于本文件中的一般性建议。保持本文件精简；项目架构或必跑检查发生变化时同步更新。

## 权威资料

- 修改业务行为前先阅读 `README.md`，它是当前产品范围和业务契约。
- 修改草稿、文件、OCR、Excel、OA 提交、异常恢复或清理流程前，先阅读 `docs/DINGTALK_OA_INTEGRATION_DESIGN.md`，它是当前状态机和集成设计。
- `docs/V1_IMPLEMENTATION_PLAN.md` 只作为历史规划背景。如果它与当前 README、设计文档、迁移或测试冲突，不得把它当作当前契约。
- 不要仅凭截图推断业务规则。截图用于说明界面问题，用户的文字要求和当前业务契约决定实际行为。

## 架构与不变量

- 前端使用 Vue 3 + TypeScript；后端使用 FastAPI、SQLAlchemy/Alembic 和 SQLite；生产环境通过 Nginx 与 Docker Compose 提供服务。
- `/` 和 `/m` 是同一报销流程的两种展示。`/m` 通过显式 `mobile` prop 启用移动端展示，不要根据视口宽度推断产品模式。
- 桌面端和移动端可以采用不同布局，但适用的业务信息、状态、校验和操作必须一致。修改任一渲染分支时，都要检查另一分支，并为共享行为新增或更新桌面端/移动端参数化测试。
- 移动端设置页必须返回 `/m`，桌面端设置页必须返回 `/`。平台适配方式可以不同：移动端文件选择和 Excel 打开使用钉钉或系统原生流程，但业务结果和错误处理应与桌面端一致。
- SQLite 中已保存的数据是权威数据；Pinia 保存当前编辑状态。所有读写都必须保留 revision/CAS 校验，以及草稿所属企业、员工和部门的隔离。
- 身份、部门、业务校验、汇总金额、补助计算、文件完整性、锁定快照和 OA 提交均以后端为准，不得改为信任客户端传入值。
- 任何可能阻断流程的持久状态，都必须能由系统安全恢复，或者向用户显示真实且可执行的处理方式。服务器状态为失败、过期、未解决或仍在运行时，不得显示为已完成。
- OA 创建结果不确定时，必须通过回查或带审计证据的人工恢复处理。不得仅因首次响应丢失或超时就再次创建 OA 实例。

## 桌面端与移动端一致性检查

修改报销页或设置页时，要在两个展示模式中检查所有受影响能力：

- 审批查询、选择、多部门处理，以及自动带入报销单的字段；
- 是否申请补助、逐审批补助时段、计算、警告和合计；
- 材料上传、重复文件跳过、批次进度、清空、重试、确认或修改用途、预览、手工新增费用、编辑、关联证明和删除；
- 未完成、失败、过期、已保存、已锁定、提交中、结果不确定、已恢复和已完成状态；
- Excel 预览或打开、设置页往返、最终 OA 提交、进度刷新、异常恢复及提交后的操作。

优先复用状态和事件处理逻辑。如果桌面端和移动端必须使用不同模板，重复部分只能是展示层，并通过参数化测试覆盖共享行为。

## 文件、OCR 与提交流程

- 保持 README 和 OA 设计文档规定的文件类型、大小与页数限制、哈希、生命周期、用途规则及来源/证明材料关系。
- 批量材料按单个文件独立处理：一个文件失败、超时、重复、取消或繁忙，不得重放或丢弃已经成功的文件。失败文件在影响预览或提交前，必须可以恢复、移除或明确解决。
- 除非同时重新设计资源模型和并发测试，否则保留单个 OCR 执行槽及有界 FIFO 准入队列。队列在不同用户之间保持公平；队列容量不等于系统支持的用户数量。
- 清空、重试、预览、锁定、提交和异常恢复等破坏性或状态敏感操作前，要重新读取服务器当前状态。revision 冲突应通过最新状态进行协调，不要显示误导性的通用“其他页面已更新”错误。
- Excel 预览与最终 OA 提交必须校验同一份已保存业务内容。不得从看似完整的结果中静默遗漏未解决的来源文件或未完成费用行。
- 保持幂等性、不可变提交快照、附件顺序、严格 OA 回读、清理检查点及恢复审计记录。

## 鉴权、隐私与安全

- 所有业务 API 都要求已认证 Session；所有写操作都要求 CSRF 校验；管理员权限必须由后端强制执行。
- 钉钉密钥只能保存在后端。不得暴露 staging 目录或原始 OCR 文本，不得在日志中记录凭据、原始 OCR 内容、真实员工数据、审批标识、项目、票据或生产文件哈希。
- 测试只能使用合成或彻底脱敏的 fixture。私密导出和诊断文件必须使用仓库已忽略的私密文件名规范。
- 不得为了让测试通过而放宽模板校验、文件校验、归属检查、迁移保护、readiness 检查或 OA 回读。
- 工作区有未提交修改时要保留无关内容。除非用户明确要求，不得 commit、push、部署或修改真实钉钉/OA 环境。

## 数据库迁移

- 始终保持唯一 Alembic head。本文件创建时的 head 为 `20260915_0019`；实际值应执行 `cd backend && uv run --frozen --extra dev alembic heads` 获取，不得只信任本句。
- 数据库结构变更通常需要同步更新 SQLAlchemy model、必要的 model export、Alembic revision、迁移测试、readiness 的 revision/table/column 预期、测试数据库 revision 初始化，以及 `README.md` 和 `docs/DINGTALK_OA_INTEGRATION_DESIGN.md` 中的当前迁移说明。
- 同时测试受支持旧数据的升级路径和空数据库升级路径。若降级会静默丢失业务记录、审计证据、锁定快照或文件关系，必须拒绝降级。
- 生产迁移前必须对同一时点的 SQLite 和 staging 一致性备份。

## 验证要求

开发过程中先运行与修改直接相关的最小测试，再根据改动影响扩大检查范围。使用仓库命令，不要用临时命令替代：

```bash
make backend-test
make backend-lint
make frontend-test
make frontend-typecheck
make frontend-lint
make frontend-build
make compose-config-development
make nginx-policy-check
```

跨层、数据库结构、状态机、安全、OCR、Excel 或 OA 提交相关改动，应运行上面所有适用检查。仅在具备生产必需配置时运行 `make compose-config`。修改 OCR 制品、运行时、Docker 镜像或部署行为时，还要运行 `make ocr-models-check` 及相关 OCR/镜像 smoke 检查。

- 前端行为变更需要有意义的 Vitest 覆盖；桌面端与移动端共享行为必须测试两种模式。
- 后端 API 或 service 变更应按实际影响覆盖成功、拒绝、鉴权/归属、revision 冲突、幂等和异常恢复。
- 数据库迁移应覆盖空数据库、受支持升级路径、当前 head/readiness 检查，以及可能丢失数据时的降级保护。
- 修改完成后运行 `git diff --check`。没有在当前工作区实际运行的检查，不得声称已经通过。

## 文档维护

- 产品契约、支持行为、限制、部署基线或 migration head 变化时更新 README。
- 状态转换、持久化、异常恢复、安全边界、外部 API 行为或清理流程变化时更新 OA 集成设计。
- 只记录已经实现的行为。未实际执行时，不得声称已经完成浏览器自动化、生产验证、模型可用性检查或外部钉钉/OA 验证。
